<h1>Edit to_do</h1>
<%= form_for :todo, url: todo_path(@todo), method: :put do |f| %>

	<p>
		<%= f.label :title %><br />
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :text %><br />
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>